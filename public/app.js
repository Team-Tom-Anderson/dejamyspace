(function() {
   angular.module('app', ['ui.router'])
       .component('main', {
           controller: controller,
           templateUrl: 'mainView.html'
       })


   function controller() {
     const vm=this;

   }

})();
