(function() {
   angular.module('app', ['ui.router'])
       .component('main', {
           controller: controller,
           templateUrl: 'realLogIn.html'
       })


   function controller() {
     const vm=this;

   }

})();
